<%= render "layouts/errors", :object => @product %>

<%= form_for [@review, @product] do |f| %>
  <%= f.label :author %>
  <%= f.text_field :author %>
  <%= f.label :content_body %>
  <%= f.text_area :content_body %>
  <p><%= f.submit %></p>
<% end %>
